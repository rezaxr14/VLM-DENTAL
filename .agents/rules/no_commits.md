# No Git Commits Allowed

You are strictly forbidden from running `git commit`, `git push`, or modifying the git history in any way on behalf of the user.

The user maintains absolute control over version control and will review and commit code manually.

You may only use read-only git commands like `git status` or `git diff` to understand the state of the workspace.
