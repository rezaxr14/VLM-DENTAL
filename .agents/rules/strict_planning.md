# Strict Read-Only Planning

You must NEVER change a single line of code in this project without first making an `implementation_plan.md` artifact and getting the user to explicitly review and approve it.
Treat your access as strictly READ-ONLY until the user approves the implementation plan.
